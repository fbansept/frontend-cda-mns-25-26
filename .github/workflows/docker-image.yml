name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Extract package name and version
      id: package-info
      run: echo "VERSION=$(jq -r '.version' package.json)" >> $GITHUB_ENV

    - name: Log in to Docker Hub
      uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push the Docker image
      run: |
        docker build -t fbansept/demo-cda-26-front .
        docker push fbansept/demo-cda-26-front
        
    - name: Tag and push the Docker image
      run: |
        docker tag fbansept/demo-cda-26-front fbansept/demo-cda-26-front:${{ env.VERSION }}
        docker push fbansept/demo-cda-26-front:${{ env.VERSION }}
